@model IEnumerable<TrashCollector.Models.Employee>

@{
    /**/

    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmployeeNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ZipCode)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.ApplicationEmployeeId)
        </th>*@
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ZipCode)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.ApplicationEmployeeId)
            </td>*@
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>

<!--
    TODO:
     - Find Milwauke on the Map https://www.latlong.net/place/milwaukee-wi-usa-2798.html
     - Get Google API KEY from https://developers.google.com/maps/documentation/javascript/get-api-key
     - Use the .address method with Maps API to build an address to search instead of longitude and latitude
    Source: https://www.w3schools.com/graphics/google_maps_basic.asp
    Reference: https://www.youtube.com/watch?v=xBBO1DaXiUc
-->
<div id="googleMap" style="width:100%; height:400px;"></div>

<script>
    function showCustomerAddressOnMap() {
        var mapSettings = {
            center: new google.maps.LatLng(43.038902, -87.906471),
            zoom: 5,
        };
        var map = new google.maps.Map(document.getElementById("googleMap"), mapSettings);
    }
</script>

<script src="https://maps.googleapis.com/maps/api/js?key=INSERTGOOGLEMAPAPIKEYHERE&callback=showCustomerAddressOnMap"></script>